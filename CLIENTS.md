# Clients

Each top-level client folder is intended to be independently usable and releasable.

- `typescript`: TS/JS runtime wrapper.
- `python`: Python wrapper.
- `go`: Go wrapper.
- `react`: React helpers depending on `@thumbrella/client`.
- `astro`: Astro helpers depending on `@thumbrella/client`.
